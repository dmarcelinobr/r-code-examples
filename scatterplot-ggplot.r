# Basic Scatterplot in ggplot2# original source: ggplot2 book# load the ggplot2 librarylibrary(ggplot2)# set the seed so we can reproduce the resultsset.seed(1410)# create a variable that is the first 100 rows of the diamonds datasetdsmall <- diamonds[sample(nrow(diamonds), 100), ]# create a scatterplot of carat vs. pricep <- qplot(carat, price, data = diamonds)# view the scatterplotp# create a scatterplot with the color determined by the diamonds colorp2 <- qplot(carat, price, data = dsmall, colour = color); p2# create a scatterplot withe the point shape determiend by the cutp3 <- qplot(carat, price, data = dsmall, shape = cut); p3# create a scatterplot with the point obacity reducedp4 <- qplot(carat, price, data = diamonds, alpha = I(1/200)); p4# create a scatterplot with a smootling linep5 <- qplot(carat, price, data = dsmall, geom = c("point", "smooth")); p5# create a scatterplot with a smoothing line but no standard error conep6 <- qplot(carat, price, data = dsmall, se = FALSE, geom = c("point", "smooth")); p6